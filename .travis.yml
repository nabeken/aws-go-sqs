sudo: false
language: go

go:
  - tip
  - 1.14.x

script:
  - go build ./...
  - go test -short -v ./...

after_script:
  - F=$(go fmt ./...); if [ ! -z "$F" ]; then echo "Please gofmt for"$'\n'"$F"; exit 1; fi
